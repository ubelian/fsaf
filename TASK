
С использованием исключительно C++ и STL разработайте class, реализующий хранилище товаров.

Хранилище целиком хранится в оперативной памяти.

Запись хранилища содержит поля: уникальный артикул – строка производитель товара – строка наименование товара – строка цена – целое число

Необходимо реализовать операции: добавить товар в хранилище. Сложность O(log(N)), N – текущий размер хранилища получить 
товар с заданным артикулом. Сложность в среднем случае – O(1), сложность в худшем случае – O(N) получить 
множество товаров заданного производителя. Сложность в среднем случае – O(log(N)), сложность в худшем случае – O(N)
